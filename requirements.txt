# core (engine, DeepCFR training, evaluation, pygame app)
numpy
torch>=2.4
treys
tqdm
tensorboard
pybind11>=2.12   # to build the optional C++ extension: python setup.py build_ext --inplace
# tests
pytest
